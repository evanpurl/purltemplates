[
  {
    "UpdateStageName": "Create Virtual Environment",
    "UpdateSourcePlatform": "Windows",
    "UpdateSource": "Executable",
    "UpdateSourceData": "cmd",
    "UpdateSourceArgs": "mkdir '{{$PATH}}venv' && python -m venv '{{$PATH}}venv'"
  },
  {
    "UpdateStageName": "Install Requirements",
    "UpdateSourcePlatform": "Windows",
    "UpdateSource": "Executable",
    "UpdateSourceData": "cmd",
    "UpdateSourceArgs": "'{{$PATH}}venv/bin/activate' && python3 -m pip install --upgrade pip && python3 -m pip install -r '{{$PATH}}requirements.txt'"
  }
]